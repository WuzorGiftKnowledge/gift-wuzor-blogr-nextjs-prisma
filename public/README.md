# Public Assets

Place your logo image file here as `logo.jpg` or `logo.jpg`.

The logo should be named:
- `logo.jpg` (preferred)
- or `logo.jpg`

The Logo component will automatically use this image throughout the application.

